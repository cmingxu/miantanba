<% @activities.each do |activity| %>
<dl>
  <dt><%= activity.title %>
  <%=activity.locale.area.city.name %>-
  <%=activity.locale.area.name %>-
  <%=activity.locale.name %>


  </dt>
  <dd><%= activity.start_time %></dd>
</dl>
<% end %>