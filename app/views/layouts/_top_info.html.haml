.top_info
  %ul
    - if current_user.logged?
      %li/
    %li= link_to "帮助",help_path
    - if current_user.logged?
      %li
        = current_user.user.login
        ,进入#{link_to "个人中心", profile_path} #{link_to "退出",logout_path}
    - else
      %li
        = link_to "登录", login_path
        = link_to "注册", signup_path
