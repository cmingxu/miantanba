#pri
#sec
  .calendar_body
    .fl
      %ul
        - @areas.each do |area|
          %li
            = area.name
    .fr
      %table.calendar_table_head{:cellpadding => "0", :cellspacing => "0"}
        %tbody
          %tr
            %th
              <
            - calendar_head.each do |key,value|
              %th{:title => key, :width => "13%"}
                %div= value
            %th
              >
      %table.calendar_table_body{:cellpadding => "0", :cellspacing => "0"}
        %tbody
          %tr
    .clear
.clear
